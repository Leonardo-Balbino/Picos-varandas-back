# apps/web — reservado

Ainda não iniciado. Quando o Card L1 começar, esta pasta recebe a SPA
(Vite + React + TypeScript, ou Vue 3 + Composition API — decisão pendente,
ver seção "Decisão pendente — framework" do Card L1 na especificação).

Mantida vazia (apenas este README) para que a estrutura de monorepo descrita
na seção 3.1 do documento já exista desde o Card A1.
