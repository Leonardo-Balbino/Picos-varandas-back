import { Module } from '@nestjs/common';

/**
 * Parser de extratos (D1), painel duplo (D2), conciliação manual (D3) e
 * motor de match automático (D4). Placeholder do esqueleto modular do
 * Card A1.
 */
@Module({})
export class ConciliacaoModule {}
