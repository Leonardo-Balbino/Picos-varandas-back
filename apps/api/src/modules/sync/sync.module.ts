import { Module } from '@nestjs/common';

/**
 * Ingestão de vendas do PDV via agente local (I1) e heartbeat /
 * monitoramento do agente (I2). Placeholder do esqueleto modular do
 * Card A1.
 */
@Module({})
export class SyncModule {}
