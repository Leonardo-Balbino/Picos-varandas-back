import { Module } from '@nestjs/common';

/**
 * Endpoint agregador de KPIs (Card C1) e séries temporais / distribuição por
 * meio de pagamento (Card C2). Placeholder do esqueleto modular do Card A1.
 */
@Module({})
export class DashboardModule {}
